#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "elim.h"

void eliminar(char *str, char *pat) {
   char *ptr_i = str, *ptr_j = str;
   size_t len = strlen(str);
   char temp;
   while ((ptr_i < len) || (ptr_j < len)) {
      
   }
}

char *eliminados(char *str, char *pat) {
   return pat;
}
